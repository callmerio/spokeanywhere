# Handoff - 2026-04-29

## Executive Summary

SpokenAnyWhere is in Maestro milestone `M2`:

- `M1` / Phase 1 is completed and validated.
- `M2` is in progress.
- The current workstream is release hygiene, workflow consolidation, commit grouping, and non-destructive old-worktree harvest.
- Destructive cleanup is explicitly held back until user approval.

The next agent should continue from M2, not restart discovery from scratch.

## Current Workspace

Use this path for all build/test commands:

```bash
cd /Volumes/1TBSSD/offload/Workspace/macos/spokeanywhere/spoke
```

Important: earlier failed tests came from running SwiftPM in the shadow path:

```bash
/Users/bigdan/Workspace/macos/spokeanywhere
```

That path does not contain the active `spoke/Package.swift` in the expected position. Treat "Could not find Package.swift" from that path as an execution-directory error, not a code failure.

## State Files To Read First

Read in this order:

1. `.workflow/state.json`
2. `.workflow/codebase/action-logs/phase-1-execution-report-2026-04-27.md`
3. `.workflow/codebase/action-logs/main-worktree-split-2026-04-27.md`
4. `.workflow/codebase/action-logs/m2-harvest-report-2026-04-27.md`
5. `.workflow/codebase/action-logs/m2-autoresearch-lessons-harvest-2026-04-27.md`
6. `.workflow/codebase/action-logs/m2-cleanup-readiness-2026-04-27.md`
7. `.workflow/roadmap.md`

Known inconsistency:

- `.workflow/state.json` is the fresher state source and marks `M1` as `completed` and `M2` as `in_progress`.
- `.workflow/roadmap.md` still has a stale progress table that says Phase 1 is "Not started". Update it later as a docs hygiene task; do not let that stale row override `.workflow/state.json`.

## Completed Work

### Phase 1 / M1

Completed through `PLN-001` / TASK-001 through TASK-006.

Validated from `/Volumes/1TBSSD/offload/Workspace/macos/spokeanywhere/spoke`:

```bash
swift test --filter LiveCaption
swift test --filter CaptionLineBufferTests
swift test --filter AppLifecyclePlanTests
swift test --filter AppRuntimeHelpersTests
swift build
```

All passed.

Evidence:

- `.workflow/codebase/action-logs/phase-1-execution-report-2026-04-27.md`

### M2 Harvest

Completed non-destructive harvest passes:

- QuickAsk/status menu polish branch classified as mostly absorbed.
- Screen-capture indicator attribution lesson migrated into `.workflow/specs/debug-notes.md`.
- `autoresearch-lessons.md` from `autoresearch/arch-v3-20260410-030315` harvested into Maestro specs.
- Cleanup readiness report generated.
- SelectionToolbar startup permission coverage ported from `spoke-swiftui-wave1` into current main.

Evidence:

- `.workflow/codebase/action-logs/m2-harvest-report-2026-04-27.md`
- `.workflow/codebase/action-logs/m2-autoresearch-lessons-harvest-2026-04-27.md`
- `.workflow/codebase/action-logs/m2-cleanup-readiness-2026-04-27.md`

## Code Changes Ported From Old Worktree

Ported from dirty `spoke-swiftui-wave1` worktree, without merging that branch wholesale:

- `spoke/App/AppDelegate.swift`
- `spoke/Services/SelectionToolbarRuntimeHelpers.swift`
- `spoke/Services/SelectionToolbarManager.swift`
- `spoke/Tests/SelectionToolbarStartupPermissionTests.swift`

Behavioral intent:

- When SelectionToolbar is enabled at startup, call:

```swift
selectionToolbarManager.start(requestPermissionIfNeeded: true)
```

- Accessibility permission alert text should use the runtime app name, e.g. `SpokenAnyWhere Dev`, instead of hardcoding only `SpokenAnyWhere`.

Validation already passed:

```bash
swift test --filter SelectionToolbarStartupPermissionTests
swift test --filter SelectionToolbar
swift build
```

## Current Dirty Worktree Shape

The worktree is intentionally dirty and mixed. Do not stage everything together.

Main groups from `.workflow/codebase/action-logs/main-worktree-split-2026-04-27.md`:

1. `maestro-workflow-docs`
2. `product-roadmap-docs`
3. `live-caption-runtime-debug-tests`
4. `app-runtime-helper-seams`
5. `legacy-ccw-cleanup`
6. `Hold / Do Not Stage Yet`

Additional file from this handoff:

- `.workflow/codebase/action-logs/handoff-2026-04-29.md`

Recommended commit grouping:

| Candidate | Include | Validation |
|-----------|---------|------------|
| `chore(maestro): initialize workflow docs and codebase map` | `.workflow/config.json`, `.workflow/state.json`, `.workflow/project.md`, `.workflow/roadmap.md`, `.workflow/codebase/`, `.workflow/specs/`, `.workflow/scratch/` | JSON validation for `.workflow/**/*.json`; file existence checks |
| `fix(selection-toolbar): request accessibility permission at startup` | `AppDelegate.swift`, `SelectionToolbarRuntimeHelpers.swift`, `SelectionToolbarManager.swift`, `SelectionToolbarStartupPermissionTests.swift` | `swift test --filter SelectionToolbar`; `swift build` |
| `feat(debug): add live caption debug simulation runtime` | Live Caption debug helper/script subset | `swift test --filter LiveCaptionDebugSimulationRuntimeHelpersTests`; `bash -n spoke/scripts/debug/livecaption-mock-stream.sh` |
| `fix(live-caption): stabilize collapsed focus debug runtime` | Live Caption runtime/view/test subset | `swift test --filter LiveCaption`; `swift test --filter CaptionLineBufferTests` |
| `refactor(app): extract runtime helper seams` | AppDelegate/AppRuntime helper seam subset | `swift test --filter AppLifecyclePlanTests`; `swift test --filter AppRuntimeHelpersTests`; `swift build` |
| `docs: refresh product roadmap around desktop context workflows` | `ROADMAP.md`, `ROADMAP.detail.md`, selected `docs/superpowers/` feature-history docs | file existence and source-priority review |
| `chore: retire legacy CCW agent framework` | `.agent/`, `.workflow/.ccw-session/`, `.workflow/.lite-fix/` deletions | only after migration confidence; do not mix with Swift code |

Hold list:

- `.draft.md`
- `ROADMAP.md.bak-*`
- `New Document.tldr`
- `RoadMap.tldr`
- `mindmap.excalidraw`
- `ttsdemo.md`

Do not stage hold-list files until they are inspected or explicitly classified.

## Cleanup Readiness

Ready for approval, but not executed:

- `git worktree prune` for 8 stale admin entries.
- Remove `fix/screen-capture-indicator` worktree/branch after confirming no need to keep `spoke/.draft.md`.

Nearly ready:

- `plan-quickask-settings-menu-polish-20260416`
  - Remaining step: preserve its plan doc or mark it superseded.
- `autoresearch/arch-v3-20260410-030315`
  - Remaining step: decide whether any docs outside `autoresearch-lessons.md` need archival.

Not ready:

- `spoke-swiftui-wave1`
  - SelectionToolbar dirty patch was harvested, but the branch still has 14 unique commits and many UI/test diffs.
  - Do not delete until remaining diffs are reviewed as obsolete or separately harvested.
- `codex/nocturne-memory-governance`
  - Treat through memory governance, not app cleanup.
- `autoresearch/control` and nested manual/autoresearch chain
  - Broad stale research chain; only partial harvest completed.

## Do Not Do Without Explicit User Approval

Do not run:

```bash
git worktree prune
git worktree remove <path>
git branch -d <branch>
git reset --hard
git checkout -- <path>
rm -rf <path>
```

Do not merge old branches wholesale:

- `spoke-swiftui-wave1`
- `plan-quickask-settings-menu-polish-20260416`
- `autoresearch/arch-v3-20260410-030315`
- `autoresearch/control`
- `codex/nocturne-memory-governance`

## Recommended Next Execution Order

Continue automatically for non-destructive tasks:

1. Preserve or mark superseded `plan-quickask-settings-menu-polish-20260416` plan doc.
2. Review remaining `spoke-swiftui-wave1` diffs by file group; classify as obsolete, already absorbed, or harvest candidate. Do not merge.
3. Generate a split-commit staging plan from the current dirty tree.
4. Run full gates from `spoke/`:

```bash
swift build
swift test
bash Tests/run-concurrency-check.sh
```

5. If gates pass, prepare commits in the split order above.
6. Only after commit boundaries are clear, ask for destructive cleanup approval.

## Auto-Advance Policy

User preference: continue driving execution without asking at every phase.

Allowed without further confirmation:

- read-only audits
- diff/classification
- writing/updating non-destructive action logs
- JSON validation
- Swift build/test commands
- focused bug fixes that are covered by tests and stay inside current scope

Pause only for:

- destructive Git/filesystem operations
- discarding or overwriting uncommitted work
- merging stale branches
- changing the agreed scope
- repeated failures where the next step requires product/architecture judgment

## Verification Commands For Next Agent

Use these from repo root:

```bash
python3 -m json.tool .workflow/state.json >/dev/null
python3 -m json.tool .workflow/config.json >/dev/null
python3 -m json.tool .workflow/codebase/doc-index.json >/dev/null
```

Use these from `spoke/`:

```bash
swift test --filter SelectionToolbar
swift test --filter LiveCaption
swift test --filter CaptionLineBufferTests
swift test --filter AppLifecyclePlanTests
swift test --filter AppRuntimeHelpersTests
swift build
```

Full gates before final release/cleanup decisions:

```bash
swift test
bash Tests/run-concurrency-check.sh
./dev.sh
```

## Latest User-Facing Report

A local HTML recommendation report was generated at:

```text
/Users/bigdan/.codex/temple/user-reporter/2026-04-27/2026-04-27_22-42-17-spokeanywhere-m2-next-recommendation.html
```

It recommended option `A`: continue M2 non-destructive closeout first, then full gates, then cleanup approval.

## Current Recommendation

Proceed with M2 closeout:

1. QuickAsk polish plan preserve/superseded decision.
2. `spoke-swiftui-wave1` remaining diff classification.
3. Split-commit plan.
4. Full gates.
5. Approval-gated cleanup.

Do not spend time revalidating Phase 1 basics unless files in those slices changed after this handoff.

---
Generated on 2026-04-29 for the next SpokenAnyWhere coding agent.
