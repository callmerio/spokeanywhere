# Tech Stack

## Overview

SpokenAnyWhere is a native macOS 14+ productivity app written in Swift 5.9, built with Swift Package Manager and packaged as a faceless menu-bar-style app via Swift Bundler.

## Core Stack

| Area | Technology | Evidence |
|------|------------|----------|
| Language | Swift 5.9+ | `spoke/Package.swift:1` |
| Platform | macOS 14+ | `spoke/Package.swift:5` |
| UI | SwiftUI + AppKit | `spoke/App/SpokenlyApp.swift`, `spoke/UI/` |
| Persistence | SwiftData | `spoke/App/SpokenlyApp.swift`, `spoke/Core/DataModels.swift` |
| Packaging | Swift Bundler | `spoke/Bundler.toml` |
| Logging | os.Logger | subsystem `com.spokeanywhere` |
| Tests | Swift Testing + XCTest | `spoke/Tests/`, `spoke/Tests/UITests/` |

## Apple Frameworks

- AVFoundation/CoreAudio/Speech for recording and transcription.
- ScreenCaptureKit/Vision/ImageIO/UniformTypeIdentifiers for screen capture and OCR.
- Carbon/ApplicationServices for hotkeys and accessibility-related behavior.
- CoreML for screenshot image enhancement support.
- WebKit for selectable markdown/answer rendering.

## Dependencies

- `swift-identified-collections` from Point-Free via Swift Package Manager.

## Build and Validation

Run commands from `spoke/`:

- `swift build`
- `swift test --parallel`
- `./Tests/run-concurrency-check.sh`
- `./dev.sh` for bundled, signed, TCC-sensitive launch validation.

## Key Config Files

- `spoke/Package.swift` — SPM targets, dependencies, source paths, tests.
- `spoke/Bundler.toml` — bundle ID, LSUIElement, TCC usage strings.
- `spoke/dev.sh` and `spoke/scripts/dev-run.sh` — dev packaging/signing/logging flow.

---
*Auto-generated by codebase-rebuild at 2026-04-27T00:00:00Z*
