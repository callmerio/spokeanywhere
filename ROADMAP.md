# SpokenAnyWhere Product Roadmap (Base Draft)

> 版本: `v2026.04-base`
> 更新时间: `2026-04-18`
> 产品主叙事: `桌面理解层 + 工作流层`
> 优先级: `跨应用流转 -> 看屏即问`
> 说明: 旧的工程 / 执行版 roadmap 已归档到 [`spoke/docs/roadmap/2026-02-execution-roadmap-archive.md`](/Volumes/1TBSSD/offload/Workspace/macos/spokeanywhere/spoke/docs/roadmap/2026-02-execution-roadmap-archive.md)

---

## 0. 产品定位

SpokenAnyWhere 不再只被定义为“语音转写 + AI 精炼工具”，而是一个围绕桌面上下文工作的助手。

它的核心价值不是单独把某一种输入做强，而是把用户当前看到、听到、选中的内容快速转成可操作的上下文，然后把这些上下文送进 AI、消息面板、贴屏文本和后续工作流。

一句话定义：

> **SpokenAnyWhere 帮用户把桌面上正在发生的内容，变成可以立即提问、加工、记录和流转的上下文。**

当前主用户可暂时定义为三类：

- `高频知识工作者`：在浏览器、文档、聊天工具、会议工具之间频繁切换，需要快速提问、翻译、总结、摘录。
- `双语 / 多语信息工作者`：依赖实时字幕、词典、翻译与屏幕内容理解能力。
- `重上下文创作者`：需要把看到的、听到的和处理后的内容快速沉淀成桌面对象或工作流输入。

当前阶段的产品边界：

- 我们要做的是 `桌面上下文助手`
- 不是独立的长文笔记软件
- 不是通用自动化平台
- 也不是只围绕录音的单点语音工具

---

## 1. 当前功能地图

### 1.1 桌面采集层

这一层的职责，是把“桌面上正在发生什么”稳定地拿进系统。

当前已具备的能力：

- `区域截图`：用户可直接截取当前屏幕局部区域，并进入后续标注、Pin、OCR 或 AI 处理链。
- `OCR / 文本抽取`：截图后的图像可以转成文本，成为后续提问、总结和流转的原材料。
- `文本选择监控`：在任意应用中选中文本时，可以触发选择工具栏和后续动作。
- `剪贴板注入`：读取当前剪贴板内容，并送入消息面板或后续上下文处理链。
- `贴屏文本（PinnedText）`：把剪贴板文本或其他文本来源转成可拖动、可编辑、可长期悬浮的桌面对象。

当前判断：

- `区域截图 + OCR` 已经是成熟入口
- `贴屏文本` 还在快速成长，但非常可能成为后续产品支点
- `剪贴板` 当前更像能力底座，还没有完全产品化成清晰的入口故事

### 1.2 桌面理解层

这一层的职责，是把采集来的内容转成“用户当下能理解、能提问、能判断价值”的上下文。

当前已具备的能力：

- `Quick Ask 上下文拼装`：把 OCR、剪贴板、实时字幕等来源拼进同一次提问里。
- `实时字幕 / 翻译`：把正在听到的内容转成字幕，并支持翻译与词汇辅助理解。
- `词典 / 查词 / 生词`：对单词、短语提供解释、收藏与词汇管理。
- `Workflow profile / prompt 选择`：不同上下文可路由到不同 AI 配置、模型或处理方式。
- `屏幕内容转文本后的 AI 处理`：对屏幕内容、转写结果或提问内容做解释、总结、精炼等。

当前判断：

- `理解层` 已经开始成型，但还缺一个更统一的“上下文包”抽象
- 现在的理解能力更像多个强能力点并列存在，后续要收束成统一体验

### 1.3 桌面动作层

这一层的职责，是把理解后的结果变成用户可以立刻消费和继续流转的动作。

当前已具备的能力：

- `Quick Ask / Answer Panel`：对当前上下文提问，获得回答并继续追问。
- `Message Panel`：承接剪贴板、上下文片段和处理结果，形成更稳定的操作面板。
- `选择工具栏动作`：对已选文本快速执行翻译、查词、AI 处理等动作。
- `截图标注 / Pin / 导出`：截图不仅能看，还能编辑、标记、固定在桌面上。
- `贴屏文本编辑 / 锁定 / 标记 / 关闭`：把处理结果变成桌面上的长期对象，而不只是一次性输出。

当前判断：

- `动作层` 是产品差异化的真正开始
- 但目前不同入口对应的动作语言还没有完全统一
- 后续要把动作收束到一套稳定语义，而不是每个入口有自己的一套玩法

### 1.4 全局入口层

这一层的职责，是让产品在跨应用环境里随时可触发，而不是要求用户先“打开某个 app 页面”。

当前已具备的能力：

- `全局快捷键`
- `状态栏菜单`
- `浮动 HUD / Capsule`
- `面板类窗口（Quick Ask / Message Panel / Live Caption / Screenshot / PinnedText）`
- `Settings 统一配置入口`

当前判断：

- 入口层已经很丰富，甚至略偏多
- 后续重点不应该再继续加入口，而是统一入口背后的动作语义和结果回流

### 1.5 历史与个性化层

这一层的职责，是让产品越来越理解用户，而不是每次都从零开始。

当前已具备的能力：

- `History / Session History`
- `AppRule`
- `AIProviderConfig`
- `快捷键与模型偏好`
- `截图 / 字幕 / 工具栏 / 词典等设置`
- `词典 / 生词本 / workflow 偏好`

当前判断：

- 短期看，它像设置系统
- 长期看，它更像“个性化上下文系统”
- 这层未来很可能是产品粘性的关键来源之一

### 1.6 当前最有机会打穿的 4 个主场景

结合现有代码现实与当前产品定位，我建议把现阶段最值得打穿的主场景定义为：

1. `跨应用流转`
   用户在任意 app 里拿到一个上下文，然后快速送进 AI / Message Panel / Workflow。

2. `看屏即问`
   用户看到屏幕内容后，不需要思考“该用哪个功能”，而是直接截图 / 选词 / OCR / Quick Ask。

3. `边看边记`
   用户把重要屏幕信息转成贴屏文本、上下文卡片或长期可见对象。

4. `边听边懂`
   用户在会议、视频或音频场景里依赖实时字幕、翻译和词汇解释。

当前优先级明确为：

```text
4 跨应用流转
└─ 决定工作流层是产品主轴

1 看屏即问
└─ 决定桌面理解层是第一爆点
```

---

## 2. 未来 3-6 个月执行路线

### 2.1 核心目标

未来 3-6 个月，不建议继续横向扩大量新的零散功能，而应该优先把以下主线打穿：

> **让任何桌面上下文都能被稳定采集、快速理解、统一动作化，并可靠地回流到用户当前工作流。**

如果用一句更具体的话来讲，这 3-6 个月的使命是：

> **把“跨应用流转”做成主线，把“看屏即问”做成第一爆点。**

### 2.2 路线 A：统一上下文入口

目标：

- 把截图、OCR、选词、剪贴板、贴屏文本、实时字幕这些分散入口，收束成统一的“上下文入口体验”。

建议动作：

- 统一不同入口的上下文来源表达方式，让用户知道当前 AI 处理拿到了哪些来源。
- 统一不同入口的触发后动作，例如提问、翻译、解释、总结、沉淀。
- 在视觉和交互上减少“每个功能一个小宇宙”的割裂感。

成功标志：

- 用户不需要先决定“该用截图还是选词还是剪贴板”
- 用户只要知道：“把当前上下文送进去”

### 2.3 路线 B：统一动作层

目标：

- 把产品从“很多功能入口”变成“少数稳定动作”。

建议把核心动作收束成三类：

```text
理解
├─ 提问
├─ 翻译
├─ 解释
└─ 总结

沉淀
├─ 贴屏
├─ 收藏
├─ 标记
└─ 写入历史

流转
├─ 发到 Message Panel
├─ 发到 Workflow
├─ 发到剪贴板
└─ 回到当前工作现场
```

成功标志：

- 不同上下文入口最终都汇到一套动作语言
- 用户开始记住的是动作，而不是入口

### 2.4 路线 C：统一结果回流

目标：

- 让结果不只是“看完就没了”，而是能继续存在、继续编辑、继续流动。

重点对象：

- `Answer Panel`
- `Message Panel`
- `PinnedText`
- `History`

建议：

- 明确每一种结果载体的角色，而不是它们相互重叠。
- 让用户理解：
  - 想“马上看结果”去哪里
  - 想“继续处理”去哪里
  - 想“留在桌面上”去哪里

成功标志：

- 产品不是“会给答案”
- 而是“会把结果送回用户手边”

### 2.5 路线 D：入口与设置收敛

目标：

- 让入口和设置页更像“产品界面”，而不是“能力目录”。

建议：

- 状态栏菜单、快捷键、HUD、面板入口的命名和语义保持一致。
- 设置页减少“零散功能感”，改成围绕用户任务链组织。
- 优先补齐入口的一致性，而不是继续增加新的菜单项或新的快捷键入口。

成功标志：

- 用户能更快明白“从哪里触发”
- 也能更快明白“为什么要在这里配置”

### 2.6 当前阶段不建议优先做的事

未来 3-6 个月，我建议暂缓以下方向：

- 大规模平台扩展（iOS / iPadOS）
- 新一轮大而全的模型接入竞赛
- 插件系统或复杂自动化生态
- 过度强调主题、美化或全面 UI 重绘

这些方向并非不重要，而是会稀释当前最关键的主线：

> 先把上下文理解与流转的连续体验打穿，再谈平台扩张和生态扩张。

---

## 3. 12-24 个月未来方向

### 3.1 从“上下文工具”走向“上下文系统”

如果 3-6 个月阶段把主线打穿，那么 12-24 个月的演进目标不该只是“更多功能”，而应该是：

> **让 SpokenAnyWhere 从一组上下文工具，变成一套围绕桌面上下文长期协作的系统。**

### 3.2 方向一：个人上下文图谱

未来方向：

- 不是只记住历史记录
- 而是逐步形成“用户长期上下文图谱”

可能表现为：

- 更懂用户常见应用场景
- 更懂用户常问的问题类型
- 更懂什么该贴屏、什么该只看一次、什么该进入长期历史
- 更懂哪些信息值得保留为个人桌面知识对象

价值：

- 让产品越来越像“懂用户工作现场的助手”
- 而不是每次都从一次性输入开始

### 3.3 方向二：持续桌面理解

未来方向：

- 从一次性截图 / 一次性提问
- 走向对当前桌面工作现场的持续理解

重点不是全自动监控一切，而是：

- 在用户明确授权和可控范围内
- 持续帮助理解“当前桌面上正在发生什么”

可能演进为：

- 更强的实时字幕与词汇理解
- 更强的选区、窗口、屏幕上下文关联
- 更强的“从当前桌面状态自动拼装提问上下文”

### 3.4 方向三：可组合的桌面动作系统

未来方向：

- 让动作不再停留在单次操作
- 而是可以组合成更稳定的工作流链路

例如：

- 看屏 -> 提问 -> 总结 -> 发到消息面板
- 选词 -> 查词 -> 收藏 -> 贴屏
- 实时字幕 -> 翻译 -> 提取重点 -> 保存历史

长期目标不是做一套很重的自动化平台，而是：

> **让桌面上下文可以自然流进用户自己的工作流。**

### 3.5 方向四：桌面对象化

我非常建议把 `PinnedText` 视为未来的重要支点。

为什么：

- 它是“结果回流到桌面”的第一批可见对象
- 它让处理结果不再只存在于面板里
- 它可能成为未来桌面上下文系统中的“节点”

12-24 个月的一个高价值方向，是让更多处理结果都能变成：

- 可见
- 可编辑
- 可移动
- 可保留
- 可重新送入工作流

这会让产品从“给出答案”升级成“组织桌面知识对象”。

### 3.6 方向五：真正的桌面助手气质

长期来看，我建议 SpokenAnyWhere 的终局气质不是：

- 一个截图工具
- 一个字幕工具
- 一个查词工具
- 一个转写工具

而是：

> **一个围绕桌面上下文持续协作的助手。**

它的核心不是某一条单点能力，而是：

- 理解当前桌面
- 给出适合当前桌面的动作
- 让结果持续留在用户工作现场里

---

## 4. 产品取舍原则

后续任何新需求，都建议先过这 4 个判断：

1. 它是在增强 `上下文采集` 还是 `上下文流转`？
2. 它会让主线更清晰，还是让入口更分散？
3. 它会让结果更容易回到用户手边，还是只是在增加一次性输出？
4. 它是否真的强化了“桌面理解层 + 工作流层”的产品定位？

如果这 4 个问题都答不出来，那它大概率不该进入近期路线图。

---

## 5. 下一轮细化顺序

为了便于逐段优化，建议后续按下面顺序继续：

1. `第一轮`：细化 **第 1 节 当前功能地图**
   - 校准成熟度
   - 补齐缺口
   - 明确各模块边界

2. `第二轮`：细化 **第 2 节 3-6 个月执行路线**
   - 把路线改成阶段目标 + 成功指标
   - 让它更可执行

3. `第三轮`：细化 **第 3 节 12-24 个月未来方向**
   - 把长期愿景写得更有辨识度
   - 同时避免过度承诺

> 这份文档是基础版，不追求一次写死；目标是先建立统一产品语言，再分段增强。
